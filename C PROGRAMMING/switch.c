// Program to check if a student is pass or fail based on marks!
// Pass if marks>30 ,fail if marks<=30!....all will fail!
#include<stdio.h>
int main() {
    int marks;
    printf("Enter marks : ");
    scanf("%d", &marks);
    if (marks > 30) {
        printf("The student have passed....the hard work paid off!\n");
        printf("Very Good!");}
    else {
        printf("The student have failed....look for some driving force!\n");}
    }    
    // Look at how the new line character is affecting the output (\n)...

    

